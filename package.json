{
  "name": "playwright-pdf-blob-poc",
  "version": "1.0.0",
  "description": "PoC: generate a PDF (via Playwright in Docker) with clickable download links to images served from a blob-storage-like endpoint, and measure cold-start / memory / render metrics.",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "run": "bash scripts/run.sh",
    "benchmark": "bash scripts/benchmark.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "1.49.1"
  }
}